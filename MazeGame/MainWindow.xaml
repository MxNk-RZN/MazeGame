<Window x:Class="MazeGame.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MazeGame"
        mc:Ignorable="d"
       Title="Modern Maze" 
        Height="800" 
        Width="1857"
        WindowStartupLocation="CenterScreen"
        Background="#0F0F0F"
        Foreground="White"
        FontFamily="Segoe UI"
        WindowStyle="None"
        AllowsTransparency="True"
        Focusable="True"
        FocusManager.FocusedElement="{Binding ElementName=MainGrid}">

    <Window.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#404040"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="20,12"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1A1A1A"/>
                                <Setter Property="BorderBrush" Value="#606060"/>
                                <Setter Property="Foreground" Value="#00CCFF"/>
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="15" Color="#00CCFF" Opacity="0.3" ShadowDepth="0"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#252525"/>
                                <Setter Property="BorderBrush" Value="#00CCFF"/>
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TitleText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#00CCFF"/>
            <Setter Property="FontSize" Value="28"/>
            <Setter Property="FontWeight" Value="Light"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#00CCFF" BlurRadius="10" Opacity="0.3" ShadowDepth="0"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="StatusText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="TextAlignment" Value="Center"/>
        </Style>

        <Style x:Key="InfoText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#666666"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Border Background="#0F0F0F" CornerRadius="12" Margin="10">
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="30" Opacity="0.6" ShadowDepth="2"/>
        </Border.Effect>

        <Grid x:Name="MainGrid" Focusable="True">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#151515" Padding="25,20" CornerRadius="12,12,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Style="{StaticResource TitleText}" Text="MODERN MAZE"/>

                    <Button Grid.Column="1" Content="✕" 
                            Style="{StaticResource ModernButton}" 
                            Padding="15,8" 
                            BorderThickness="0"
                            Click="CloseButton_Click"
                            ToolTip="Закрыть"/>
                </Grid>
            </Border>

            <Border Grid.Row="1" Margin="25" Background="#151515" CornerRadius="8">
                <Viewbox Stretch="Uniform">
                    <Canvas x:Name="MazeCanvas" 
                            Width="{Binding MazeWidthPixels}" 
                            Height="{Binding MazeHeightPixels}"
                            Background="Transparent"
                            MouseDown="MazeCanvas_MouseDown"/>
                </Viewbox>
            </Border>

            <Border Grid.Row="2" Background="#151515" Padding="25,20" CornerRadius="0,0,12,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" 
                              Style="{StaticResource StatusText}"
                              Text="{Binding GameStatus}"
                              FontSize="16"
                              VerticalAlignment="Center"/>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                        <Button Style="{StaticResource ModernButton}" 
                               Content="🔄 Новая игра" 
                               Command="{Binding NewGameCommand}"/>
                        <Button Style="{StaticResource ModernButton}" 
                               Content="📝 Редакт." 
                               Command="{Binding ToggleEditModeCommand}"
                               ToolTip="Режим редактирования"/>
                        <Button Style="{StaticResource ModernButton}" 
                               Content="🤖 Путь" 
                               Command="{Binding FindOptimalPathCommand}"
                               ToolTip="Найти оптимальный путь"/>
                        <Button Style="{StaticResource ModernButton}" 
                               Content="👁️ Показать" 
                               Command="{Binding ToggleOptimalPathCommand}"
                               ToolTip="Показать/скрыть оптимальный путь"/>
                        <Button Style="{StaticResource ModernButton}" 
                               Content="💾 Сохранить" 
                               Command="{Binding SaveMazeCommand}"
                               ToolTip="Сохранить лабиринт"/>
                        <Button Style="{StaticResource ModernButton}" 
                               Content="📂 Загрузить" 
                               Command="{Binding LoadMazeCommand}"
                               ToolTip="Загрузить лабиринт"/>
                        <Button Style="{StaticResource ModernButton}" 
                               Content="🚪 Выход" 
                               Command="{Binding ExitCommand}"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Orientation="Vertical" VerticalAlignment="Center" Margin="20,0,0,0">
                        <TextBlock Style="{StaticResource InfoText}" Text="Управление: WASD / Стрелки"/>
                        <TextBlock Style="{StaticResource InfoText}" Text="Цель: Достигнуть зеленой клетки"/>
                        <TextBlock Style="{StaticResource InfoText}" Text="Освещение: Ограниченное зрение" 
                                   Foreground="#00CCFF" FontWeight="Bold"/>
                    </StackPanel>

                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>